{% extends 'contact/index.html.twig' %}


{#    {%  block main %}#}
{#        {{ dump(contact_form) }}#}
{#    {%  endblock %}#}

    {% block error %}

        <div class="form-group row">
            <div class="col-sm-12">
        {% if contact_form %}
            Dane nie mogą zostać zapisane - niepoprawna domena!
        {% else %}
            Zapisano dane!
        {%  endif %}
            </div>
        </div>
    {% endblock %}

    {% block form %}

        {% if contact_form %}

            {{ form_start(contact_form) }}
            {{ form_widget(contact_form) }}

            <div class="form-group row">
                <div class="col-sm-2"></div>
                <div class="col-sm-10">
                    <input type="submit" value="Dodaj" class="btn btn-success">
                </div>
            </div>

            {{ form_end(contact_form) }}

        {%  endif %}

    {% endblock %}




